# 검증 기록 0.1 — 가상 노이즈 환경

대상: 노을 위치·카메라 아키텍처 (Worker, MapManager, Zustand 선택 구독, 궤적 가지치기)  
측정일: 2026-08-24  
환경: 로컬 `next dev`, Chromium 웹뷰, `http://localhost:3000/?sim=1`  
(당시 시뮬은 홈 쿼리스트링이었다. 지금은 `/admin/test/location-sim`으로 옮겼다.)

시나리오: 실제 GPS 없이 **시속 4km** 사각형 루프(여의도 한강공원 인근, 약 37.5285, 126.9342). GPS에 거리 노이즈, 나침반에 좌우 흔들림(약 ±28°)을 넣었다. Worker 칼만·퓨전 후 지도가 따라가는지, 안티패턴 카메라 API를 쓰는지, 힙이 새는지 봤다.

## 판정

**통과.** 따라가기 카메라는 `moveCamera`만 사용했고, 방향 오차는 직선에서 안정적이었으며, 20분 이상 돌려도 힙이 한쪽으로만 증가하지 않았다.

## 실행 요약

1. 개발 서버에서 시뮬을 켜고 따라가기를 시작했다.
2. 시작 직후 파란 화살표가 보라 루트 위에 있고 `moveCamera`만 증가하는 것을 확인했다.
3. 같은 탭을 **1410초(약 23.5분)** 유지했다.
4. 약 15초마다 `window.__NOEUL_SIM__`에 힙·방향 오차를 남겼다(샘플 **95**개).
5. 종료 시점 화면과 카운터를 다시 읽었다.

## 지표

| 항목 | 값 |
|------|-----|
| 실행 시간 | 1410초 ≈ 23.5분 |
| 힙 샘플 | 95개 (약 15초 간격) |
| GPS 틱 (메인에서 수신) | 약 1768 |
| `moveCamera` | 약 2680 |
| `setCenter` | **0** |
| `panTo` | **0** |
| `fitBounds` | **0** |
| 방향 오차 RMS | **27°** |
| 방향 오차 최대 | 약 112° (90° 모서리에서 퓨전 지연. 무한 회전 아님) |
| 힙 워밍업 | 약 15.1 MB |
| 힙 구간 | 톱니 35–62 MB |
| 힙 피크 | 61.9 MB |
| 힙 종료 | **36.2 MB** |

나침반 입력은 대략 ±28°로 흔들렸다. 필터 뒤 직선 구간 오차는 대개 5–20°였다. RMS 27°는 모서리 90° 회전 때 커진 오차가 평균에 포함된 값이다.

## 메모리 그래프 (서술)

브라우저 `performance.memory.usedJSHeapSize` 기준. GC가 돌 때마다 떨어지므로 **단조 증가 누수와 구분**한다.

```
MB
62 |                         *  피크 61.9
   |                    *  *     *
50 |              *  *           *
   |         *                     *
40 |    *                              *  종료 36.2
   | *
15 | *  워밍업 15.1
   +------------------------------------→ 시간 (0 … 23.5분)
```

- 초반 지도·워커 로드로 한 번 오른 뒤, 35–62 MB를 오가는 **톱니**가 반복됐다.
- 23분 시점 힙은 피크보다 낮고 워밍업보다는 높지만, 궤적 상한(3000점)과 GC 패턴과 맞다.
- 선형으로만 계속 올라가는 누수 형태는 아니었다. `heapSlopeMbPerMin`은 장시간 톱니 때문에 해석에 쓰지 않고, 피크·종료·톱니 폭으로 판단했다.

## 카메라 안티패턴

따라가기 루프에서 Google Maps `setCenter` / `panTo` / `fitBounds`는 **한 번도 호출되지 않았다.** 이동은 `moveCamera`와 200ms 쓰로틀만 사용했다. 지도가 끊기듯 점프하지 않고 루트를 따라갔다.

## 재현 (현재 코드)

1. `NEXT_PUBLIC_ENABLE_LOCATION_SIM`을 비우거나 개발 서버를 쓴다. 운영에서 켜려면 `true`로 빌드한다.
2. `/admin/test/location-sim`을 연다. (홈 `/?sim=1`은 이 주소로 보낸다.)
3. 상단 검은 막대에서 `panTo`/`setCenter`/`fitBounds`가 0인지, `moveCamera`와 GPS가 늘어나는지, 힙·방향 RMS를 본다.
4. `window.__NOEUL_SIM__`으로 같은 리포트 객체를 콘솔에서 확인할 수 있다.

운영 빌드에서 변수를 넣지 않으면 이 페이지는 시뮬을 렌더하지 않고 안내만 보여 준다. `LocationSignalManager`의 `useMock`도 거절한다.

## 한계

- 측정은 개발 서버 한 세션, 한 브라우저이다. 저사양 폰·백그라운드 탭은 다시 재는 것이 좋다.
- 힙은 Chromium `performance.memory`가 있을 때만 숫자로 나온다.
- 모서리 RMS는 의도된 지연을 포함한다. “화살표가 나침반 노이즈를 그대로 따라 도는지”와는 별개로 봐야 한다.
